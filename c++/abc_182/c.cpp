#include <bits/stdc++.h>
using namespace std;

int main () {
  int i, j, k, sum;
  int tmp = 0;
  string n;
  int tmp_2 = 0;
  cin >> n;

  for (i = 0; i < n.size(); i++) {
    tmp 
    for (j = 0; j < n.size(); j++) {
      if (j ==) {
        sum += int(n[j]);
      }
    }
  }
}
