#include <iostream>
#include <stdio.h>
using namespace std;

int main () {
  int n, m;

  cin >> n >> m;

  cout << n << m << endl;
//   cin >> n >> m;

//   cout << n << m;

//   scanf("%d", &n);
//   for ( i = 0; i < n; i++ ) scanf("%d", &a[i]);

//   scanf("%d", &q);
//   for (i = 0; i < q; i++ ) {
//     scanf("%d", &key);
//     if (binary_search(a,n,key)) sum++;
//   }
//   printf("%d\n", sum);
    return 0;
}
